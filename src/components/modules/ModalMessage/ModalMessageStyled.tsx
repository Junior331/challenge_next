import { styled } from '@mui/material/styles'
import { Stack } from '@mui/material'

export const StackComponent = styled(Stack)({
  lef: 0,
  bottom: '10px',
  position: 'absolute',

  '.MuiAlert-message': {
    fontSize: '14px',
  },
})
